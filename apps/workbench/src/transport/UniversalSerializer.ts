/**
 * UniversalSerializer - Central service for RDF data export.
 * Bridges to the @triplestore/io package for serialization tasks.
 */
export { UniversalSerializer } from '@triplestore/io';
export type { SerializerOptions } from '@triplestore/io';
