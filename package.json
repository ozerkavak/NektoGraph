{
    "name": "nektograph-monorepo",
    "version": "1.0.5-beta",
    "workspaces": [
        "packages/*",
        "apps/*",
        "lib_sources/*"
    ],
    "scripts": {
        "dev": "npm run dev --workspaces --if-present",
        "build": "npm run build --workspaces --if-present",
        "test": "echo 'No tests yet'"
    },
    "devDependencies": {
        "@rollup/rollup-win32-x64-msvc": "^4.60.1",
        "typescript": "^5.0.0",
        "vite": "^5.0.0",
        "vite-plugin-dts": "^3.0.0"
    }
}
